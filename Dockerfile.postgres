FROM postgres:15

# Copy the schema file with proper permissions
COPY sample_schema.sql /docker-entrypoint-initdb.d/01-schema.sql

# Set proper permissions for the SQL file
RUN chmod 644 /docker-entrypoint-initdb.d/01-schema.sql && \
    chown postgres:postgres /docker-entrypoint-initdb.d/01-schema.sql
